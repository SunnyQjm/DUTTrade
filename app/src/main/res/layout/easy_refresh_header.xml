<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@color/transparent"
    android:orientation="horizontal"
    >

    <ProgressBar
        android:id="@+id/refresh_progressBar"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:padding="3dp"
        android:visibility="invisible"
        style="?android:attr/progressBarStyle"
        android:indeterminateDuration="500"
        android:indeterminateDrawable="@drawable/progress_circle_shape"
        app:layout_constraintBottom_toBottomOf="@+id/refresh_arrow"
        app:layout_constraintEnd_toEndOf="@+id/refresh_arrow"
        app:layout_constraintStart_toStartOf="@+id/refresh_arrow"
        app:layout_constraintTop_toTopOf="@+id/refresh_arrow"
        />

    <TextView
        android:id="@+id/refresh_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:text="@string/pull_refresh"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.54"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/refresh_arrow"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginBottom="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/refresh_text"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/refresh_arrow"
        android:layout_marginRight="8dp" />

    <!--<View-->
        <!--android:layout_width="match_parent"-->
        <!--android:background="@color/line_gray"-->
        <!--android:layout_height="0.5dp"-->
        <!--app:layout_constraintBottom_toBottomOf="parent"-->
        <!--app:layout_constraintLeft_toLeftOf="parent"-->
        <!--app:layout_constraintRight_toRightOf="parent"-->
        <!--/>-->
</android.support.constraint.ConstraintLayout>